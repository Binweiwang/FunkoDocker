package common;

/**
 * Clase Longin que contiene los atributos username y password
 *
 * @param username usuario del login
 * @param password contraseña del login
 */
public record Login(String username, String password) {

}
